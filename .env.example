# Local development (npm run dev) — Vite proxies /api → localhost:5286
VITE_API_URL=/api
VITE_IDENTITY_API_URL=/identity-api

# Production build (npm run build) — see .env.production
# VITE_API_URL=http://www.sms-api-test.somee.com/api
# VITE_IDENTITY_API_URL=http://www.uc-identity-test.somee.com/api
